"""Development-only tooling.

Nothing in ``src/`` may import this package.  It holds the synthetic estate
generator and the answer keys it produces; only ``eval/`` and ``tests/`` are
allowed to read them.
"""
